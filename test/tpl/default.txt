%span
  faa="123"

%span multy="1" line={{ 3 + 4 }}

%span
  multy="1"
  line={{ 3 + 4 }}

%jakov
  test={{ 2 + 3 }}

%button onclick="fez.count -= 1" disabled={{ count == 1 }}
  \-
%span= count

%button onclick="fez.more()" disabled={{ count <= MAX }}
  +

- if count > 0
  .nested
    - for el in [1,2,3,4]
      %span.bar.baz{ class={{ el > 2 ? `foo-${el}` : null }} }= el
      - for el2 in ['foo', 'bar']
        %span.bar2.baz{ class={{ el2 > 2 ? `foo-${el2}` : null }} }= el2

  %span &mdash;

  - if count == MAX
    MAX
  - else if count == 3
    THREE
  - else
    - if count % 2
      odd
    - else
      even
